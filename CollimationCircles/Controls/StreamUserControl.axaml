<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="250" d:DesignHeight="450"
             x:Class="CollimationCircles.Controls.StreamUserControl"
             xmlns:vm="using:CollimationCircles.ViewModels"
             x:DataType="vm:StreamViewModel">

  <DockPanel
    Margin="12">
    <StackPanel
      Orientation="Vertical"
      DockPanel.Dock="Top"
      Spacing="5">
      <TextBlock
        Text="{DynamicResource Text.StreamURL}"
        VerticalAlignment="Center"/>
      <TextBox
        Text="{Binding MediaUri, Mode=TwoWay}"
        TextWrapping="Wrap"
        VerticalAlignment="Center"
        HorizontalAlignment="Stretch"/>
      <Button
        HorizontalContentAlignment="Center"
        HorizontalAlignment="Stretch"
        Content="{Binding ButtonTitle}"
        Command="{Binding PlayPauseCommand}"/>      
    </StackPanel>
    <StackPanel
      VerticalAlignment="Bottom"
      Orientation="Vertical"
      DockPanel.Dock="Bottom"
      Spacing="5">
      <TextBlock
        Text="{DynamicResource Text.ExampleHowToStartVideoStream}"
        VerticalAlignment="Center"
        TextWrapping="Wrap"/>
      <TextBox
        IsReadOnly="true"
        Text="{DynamicResource Text.StreamCommandExample}"
        VerticalAlignment="Center"
        TextWrapping="Wrap"/>
      <TextBlock
        Text="{DynamicResource Text.VideoStreamLog}"
        VerticalAlignment="Center"/>
      <TextBox
        IsReadOnly="true"
        Height="200"
        Text="{Binding MediaPlayerLog}"
        VerticalAlignment="Center"
        TextWrapping="Wrap"/>
    </StackPanel>
  </DockPanel>

</UserControl>
